name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      # Passo 1: Baixar o código do repositório
      - name: Checkout code
        uses: actions/checkout@v3

      # Passo 2: Listar arquivos para confirmar estrutura (ajuste se quiser)
      - name: Listar arquivos do projeto
        run: ls -R

      # Passo 3: Compilar e rodar testes
      - name: Compilar código Java e rodar testes
        run: |
          mkdir -p out
          # Ajuste o caminho src abaixo conforme sua estrutura
          javac -d out $(find src -name "*.java") || { echo "::error::Falha na compilação!"; exit 1; }
          java -cp out TestClass || { echo "::error::Testes falharam!"; exit 1; }
          echo "✅ Testes passaram!"

  deploy:
    needs: test
    runs-on: ubuntu-latest

    steps:
      - name: Configurar chave SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          echo -e "Host local-server\n  HostName 192.168.1.51\n  User vaio\n  IdentityFile ~/.ssh/id_ed25519\n  StrictHostKeyChecking no" > ~/.ssh/config

      - name: Deploy no servidor via SSH
        run: |
          ssh -o StrictHostKeyChecking=no vaio@192.168.1.51 "cd /c/Users/vaio/CICD/CICD && ./deploy.sh"
